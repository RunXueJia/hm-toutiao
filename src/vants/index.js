import Vue from 'vue'
// import vant from 'vant'
import { Tabbar, TabbarItem, Button, NavBar, Form, Field, CountDown, Toast } from 'vant';

Vue.use(Tabbar)
    .use(TabbarItem)
    .use(Button)
    .use(NavBar)
    .use(Form)
    .use(Field)
    .use(CountDown)
    .use(Toast)
